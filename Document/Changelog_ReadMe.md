# English Vocab Master - 工作日誌與更新紀錄 (Changelog)

## 🎯 本日重大更新重點 (2026/06/08)

### 1. 雲端儲存防禦 (Undefined Field Prevention)

- **原因**：修正因 Google Gemini 模型回傳 JSON 物件常有部分選填屬性（如 `comment`）在前端序列化時保持為 `undefined`，進而引發 Firestore 寫入崩潰的儲存失敗錯誤。
- **作法**：在所有寫入/更新 Firestore 的端點（QuizPanel、ImportPanel 等）加入深度清理過濾機制與預防性 mapping，100% 保證沒有任何物件欄位為 `undefined`，實現永久穩定存檔。

### 2. 圖片優先覆寫原則 (Image-First Precedence / Multiple Override)

- **規則**：當批次處理截圖並將單字匯入雲端時，若該單字已被收錄且已存有別的情境題目，系統將**以圖片提取的情境內容為最高優先 100% 覆寫 (Override) 舊題目**，解決新截圖題目無法覆蓋資料庫舊教材的問題。

### 3. 微秒級拼字留存交互 (Smart Input Retention)

- **優勢**：在 Quiz 答錯時，自動轉換成「保留正確字、其餘變底線」的智慧重試機制，不干擾主輸入框操作，又提供精準的字母對齊指導。

### 4. 測驗體驗流暢度升級：音標去噪與延遲語音公布 (Quiz UX Upgrades: No-Chaff Phonetics & Delayed TTS Memory reinforcement)

- **問題排除**：排除因部分裝置不支援特殊國際音標編碼，導致在測驗卡下方顯示代表亂碼的「空白方框 (Rectangle Boxes)」問題，改為完全拿掉此音標提示，保持卡片介面簡約純淨。
- **作答沉浸感優化**：預設在作答未完成前不顯露例句發音按鈕，避免提前劇透。當使用者**確認答對或公布解答**的那一霎那，播放朗讀按鈕將配合優雅的 bounce 動畫彈跳現身，引導使用者點擊整句朗讀發音，透過「答完後聆聽」加深單字在真实英文例句中的聲學語境印象。

---

## 🎯 本日重大更新重點 (2026/06/12)

### 1. 全方位常忘單字自主標記 (Contextual Star Bookmark Upgrades)

- **背景**：為了增強拼寫測驗過程中「檢討、記錄、加深記憶」的流暢度，打破原先必須跳轉回「雲端單字書」才能標記星號 (isHard) 的不便。
- **作法**：
  - **按鈕 1 - 標籤星號雙效按鈕**：測驗中隨時點擊測驗卡左上角等級標示區（如：`5級 / 難級`），即可一鍵標記/取消常忘單字，標記完成時等級字樣與星星均會響應金黃色微光與動畫。
  - **按鈕 2 - 答畢一鍵儲存卡片**：在公布正確答案或答對時，測驗卡下方自動展現精緻的輔助控制卡片，引導用戶進行「加入常忘」以便未來複習。
  - **按鈕 3 - 通關結算清單獨立星星**：在完成 10 題測驗的大滿貫結算畫面中，針對「本輪複習單字」清單中的每一項單字加裝了專屬的星星按鈕。使用者可一邊檢讀音標和中文翻譯，一邊針對拼寫不熟的單字就地加星，數據即時響應寫入本機 State 與 Firebase DB。
- **優勢**：完整貫穿「學習、作答、檢錯、收錄」的體驗閉環，讓錯題與生疏字詞在測驗期間完美無縫地沉澱回數據庫！

### 2. 精準語音拼寫狀態對齊 (Speech Recognition Custom Character Auto-sync)

- **問題**：解決語音輸入（Voice Input）辨識成功的單字，無法在獨立的字母框中正常渲染、或者是字串填入不全的情況。
- **改善**：引入內建模擬與重構的生命週期 Ref 綁定。現在當您點擊「語音輸入」並說出單字後，辨識出的英文字母將即時、精準地逐個填滿獨立拼字格，給使用者極佳的高效口說與聽力複寫輔助。

### 3. 本輪複習單字即時發音 (One-Click TTS for Review List)

- **新增**：在 10 題測驗通關後的「本輪複習單字」清單中，為每個單字右側配備了獨立的「喇叭/揚聲器」圖示按鈕 🔊。
- **好處**：當使用者檢討不熟的單字並加星時，可以直接點擊小喇叭聆聽美國英語原生發音，無需離開結算頁 or 跳轉回單字本，真正做到「學、練、音、字、意」全維聯動！

---

## 🎯 本日重大更新重點 (2026/06/20)

### 1. 單字連播「三階/雙置頂自適應導覽套件」(Three-Tier Desktop / Dual-Tier Mobile Sticky Center & Scroll Takeover)

- **背景**：在「雲端單字連播」自動循環播放模式中，頁面會自動滾動並置中目前的學習單字卡。然而，先前版本設定的置頂在手機瀏覽器上會因「Header 被滾走、瀏覽器網址列動態隱藏改變與重畫」而造成定位位移、Tabs 沒有 keep 住而被滾上去，或者與控制條相互重疊、消失。
- **改善與徹底解決版面跳動 (Zero-Jitter Architecture)**：
  - **行動端專屬內部滾動機制作業 (Mobile Y-Axis Scroll Takeover)**：針對行動版 Safari / Chrome 在滑動時自動隱藏網址列所導致的 Sticky 失控與版面跳動問題，進行了重構。將原本的頁面全域滾動改為「框架固定滿版 100dvh 高度、內部內容區塊獨立滾動」，直接把滾動權限從 Window 接管過來，確保置頂列（Sticky）完全免疫於網址列伸縮帶來的尺寸破壞效應，實現牢不可破的吸頂效果。
  - **行動端/手機端 (Mobile Adaptive Dual-Sticky)**：
    1. **Header 順滑隱藏**：為手機騰出寶貴視野高度，在手機上下滑時 Header 能隨之隱藏（設為非屬性常駐），大大舒緩螢幕窄小的視覺壓迫。
    2. **Tabs 分頁防滑接力 (sticky top-0)**：分頁 TAB 欄會 100% 機密且牢固地釘在內部滾動視窗的最頂端（`top-0`），保證絕對「keep 住」、不會跑掉或遺失。
    3. **控制面板零縫隙貼合 (sticky)**：雲端面板按鈕條則恰到好處精準接在 Tabs 正下方。
  - **電腦端/桌機端 (Desktop Three-Tier Sticky)**：
    - 主標題與頭像區（App Header）常駐於最頂部，TabsList 緊接其下，控制條完美停靠於第三層。
  - **效益**：不論在手機 Chrome、本機 PC 或者是 AI Studio 的 nested iframe 內，當單字播放機制自動滾動、或是使用者自發性滑動時，整個頂部控制面板都能毫無瑕疵、精密契合並保持在螢幕最上方，帶給學習者世界頂級的一體化播放器與單字瀏覽體驗！

### 2. 精準切換頁籤自動語音中斷與暫停 (Smart Tab-Switching Playback Auto-Pause)

- **問題**：先前在進行「雲端單字連播」自動循環朗讀時，若使用者點選「智能測驗」或「辨識萃取」等分頁（TAB），背景的 TTS 語音發音與單字自動輪播機制仍會頑固地處於活動狀態，導致強烈聲音干擾與重疊。
- **改善**：
  - **分頁激活狀態感知**：為 `LibraryPanel` 開發並注入 `isActive` 活性狀態感知屬性（與 `activeTab === "library"` 高度綁定）。
  - **強開即時中斷機制**：一旦使用者切換至「智能測驗」或「辨識萃取」頁籤，系統會即刻中斷並取消當前正在發音的所有 Web Speech TTS 任務（`window.speechSynthesis.cancel()`），並將播放狀態自動切換為「暫停」狀態，避免在測驗或進行其他任務時受到單字語音播放的干擾。
  - **效益**：多功能視窗無縫過渡，讓整個背誦、測驗、跟讀的流程極具人性化與實用性。

### 4. AI 辨識萃取針對子句與片語的深度搜索防重複保護 (Phrase Variation Search Mechanism)

- **問題**：使用者在截圖中萃取的單字（例如：APP 下方出現的字眼是 `Couple`），但在雲端資料庫當初被存成了片語 `a couple of` 或 `a couple (of)`，這時會因為字面比對不完全一致，導致系統認定該單字在雲端尚未存在，進而跳過不刪除截圖。
- **改善**：
  - **AI 關聯片語推測 (Search Variations Synthesis)**：徹底升級 API 萃取系統的 Prompt。現在 Gemini 除了會找出主單字 (word) 與 原型 (baseForm) 之外，還會透過分析圖片中的整句例句（例如圖片裡面螢光筆畫出來的片語），動態衍生出多組 `searchVariations` (如 "a couple", "a couple of") 交給前端。
  - **Firestore 優化 IN 查詢陣列**：實作中會將這組陣列與大小寫組合打平，餵給 Firestore `in` 進行嚴密的多詞彙對照搜尋。只要雲端有建檔 `a couple of` 等衍生片語，都能像雷達一樣被徹底抓出來防重複，大幅提升截圖庫整頓的乾淨度與實用性。

---

## 🎯 本日重大更新重點 (2026/06/27)

### 1. 雲端單字與辨識萃取自動補全美式音標 (Automated Phonetic Transcription Supplement)

- **背景**：許多從截圖萃取的單字由於圖片中並未包含音標，導致匯入雲端後音標欄位為空，不利於後續的發音學習與記憶。
- **改善**：
  - **辨識萃取自動補全**：在進行圖片文字辨識萃取（Extract Vocab）時，升級了 AI 提示詞。若圖片中沒有明顯的音標，系統會強制要求 Gemini 自動生成該單字的標準美式 K.K. 音標，確保每個存入雲端的單字都帶有音標。
  - **雲端單字批次補全**：在「雲端單字」面板新增了「補全音標」按鈕功能。一鍵即可自動找出雲端資料庫中缺少音標的單字，並透過全新的 `/api/gemini/generate-phonetics-batch` 伺服器端點，以批次方式請 Gemini 生成標準美式音標並寫回資料庫，大幅優化學習體驗。
