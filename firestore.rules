rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允許所有已登入用戶讀寫 vocab 集合，不限制欄位，確保標記功能暢通
    match /vocab/{docId} {
      allow read, write: if request.auth != null;
    }
    
    // 預設規則
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
